
#include "main.hpp"

int main()
{
	
	std::string cmd;
	std::cout << "Program started ...." << std::endl;
	while (true)
	{
		if (cmd == "add")
		{
		}
		else if(cmd == "search")
		{

		}
		else if(cmd == "exit")
		{

		}
	}
	
}